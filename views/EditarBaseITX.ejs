<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Editar Registro - Base ITX</title>

  <!-- ✅ Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background-color: #f3f4f6;
      font-family: 'Segoe UI', sans-serif;
    }
    .edit-container {
      max-width: 800px;
      margin: 3rem auto;
    }
    .card {
      border-radius: 1rem;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    .form-label {
      font-weight: 600;
      color: #333;
    }
    .form-control {
      border-radius: 0.5rem;
    }
    .btn-custom {
      background-color: #007bff;
      border: none;
      border-radius: 0.5rem;
      padding: 0.75rem 2rem;
      font-weight: 600;
      transition: all 0.2s ease;
    }
    .btn-custom:hover {
      background-color: #0056b3;
      transform: scale(1.02);
    }
    .header-icon {
      color: #007bff;
      font-size: 2.5rem;
    }
  </style>
</head>
<body>

  <div class="container edit-container">
    <div class="card p-4">
      <div class="text-center mb-4">
        <i class="bi bi-pencil-square header-icon"></i>
        <h3 class="mt-2">Editar Registro</h3>
        <p class="text-muted">Actualiza los datos del cliente en la base ITX</p>
      </div>

      <form action="/editBaseITX" method="POST" autocomplete="off">
        <input type="hidden" name="consecutivo" value="<%= SaleId.consecutivo %>">

        <div class="row g-3">
          <div class="col-md-6">
            <label class="form-label">Número</label>
            <input type="number" class="form-control" name="numero" value="<%= SaleId.numero %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Cédula</label>
            <input type="text" class="form-control" name="cedula" value="<%= SaleId.cedula %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Nombre</label>
            <input type="text" class="form-control" name="nombre" value="<%= SaleId.nombre %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Asesor</label>
            <input type="text" class="form-control" name="asesor" value="<%= SaleId.asesor %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Detalle</label>
            <input type="text" class="form-control" name="detalle" value="<%= SaleId.detalle %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Comentario</label>
            <input type="text" class="form-control" name="comentario" value="<%= SaleId.comentario %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Pre/Post Pago</label>
            <input type="text" class="form-control" name="pre_post_pago" value="<%= SaleId.pre_post_pago %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Fijo</label>
            <input type="text" class="form-control" name="fijo" value="<%= SaleId.fijo %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Operador Fijo</label>
            <input type="text" class="form-control" name="operador_fijo" value="<%= SaleId.operador_fijo %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Operador Móvil</label>
            <input type="text" class="form-control" name="operador_movil" value="<%= SaleId.operador_movil %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Género</label>
            <input type="text" class="form-control" name="genero" value="<%= SaleId.genero %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">WhatsApp</label>
            <input type="text" class="form-control" name="whatsapp" value="<%= SaleId.whatsapp %>">
          </div>

          <div class="col-md-6">
            <label class="form-label">Cobertura Fijo</label>
            <input type="text" class="form-control" name="cobertura_fijo" value="<%= SaleId.cobertura_fijo %>">
          </div>
          <div class="col-md-6">
            <label class="form-label">Coordenadas</label>
            <input type="text" class="form-control" name="coordenadas" value="<%= SaleId.coordenadas %>">
          </div>
        </div>

        <div class="text-center mt-4">
          <button type="submit" class="btn btn-custom me-2">
            <i class="bi bi-check-circle"></i> Guardar Cambios
          </button>
          <a href="/baseITX" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Volver
          </a>
        </div>
      </form>
    </div>
  </div>

  <!-- ✅ Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
